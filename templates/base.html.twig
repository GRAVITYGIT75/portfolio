<!DOCTYPE html>
<html lang="fr" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Portfolio Futuriste | Emmanuel Rogue{% endblock %}</title>
    <link rel="icon" href="{{ asset('favicon.ico') }}">
    
    {# Polices futuristes #}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&family=Rajdhani:wght@300;500;700&display=swap" rel="stylesheet">
    
    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
    {% endblock %}

    {% block javascripts %}
        <script src="{{ asset('scripts/app.js') }}" defer></script>
    {% endblock %}
</head>
<body class="bg-space-900 font-rajdhani text-neon-100 min-h-screen flex flex-col">
    {% include 'components/header.html.twig' %}
    
    <main class="flex-grow">
        {% block body %}{% endblock %}
    </main>
    
    {% include 'components/footer.html.twig' %}
    
    {# Effets visuels futuristes #}
    <div class="fixed inset-0 pointer-events-none">
        <div class="absolute inset-0 bg-grid-pattern opacity-5"></div>
        <div class="absolute inset-0 bg-pulse-animation opacity-10"></div>
    </div>
</body>
</html>
